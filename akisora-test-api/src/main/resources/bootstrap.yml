server:
  port: ${port-test-api}
spring:
  application:
    name: test-api
  cloud:
    nacos:
#      username: nacos
#      password: nacos
      discovery:
        server-addr: ${ip-nacos:localhost}:${port-nacos:8848}
        namespace: akisora
      config:
        server-addr: ${ip-nacos:localhost}:${port-nacos:8848}
        namespace: akisora
        file-extension: yml
        extension-configs:
          - data-id: ip.yml
            group: AKISORA_GROUP
            refresh: true
          - data-id: port.yml
            group: AKISORA_GROUP
            refresh: true
management:
  endpoints:
    web:
      exposure:
        include: "*"